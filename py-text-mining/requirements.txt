# Geordie Miner — Python dependencies
#
# Install with:   pip install -r requirements.txt
#
# Tested with Python 3.10 - 3.12.
# gensim and scipy compile native code; on Windows you may want to use
# `conda install -c conda-forge gensim` instead of pip if pip fails.
#
# Supply-chain note: these are minimum versions for convenience. For
# reproducible / production installs, freeze your environment after a
# successful install:
#     pip freeze > requirements-lock.txt
# and distribute that lock file instead.

# PDF extraction (pypdf is the actively-maintained successor of PyPDF2)
pypdf>=4.0

# Numerics
numpy>=1.26
pandas>=2.2
scipy>=1.12

# NLP / text mining
nltk>=3.8
gensim>=4.3
scikit-learn>=1.4

# Visualisation
matplotlib>=3.8
wordcloud>=1.9
networkx>=3.2

# Excel writer (used by pandas.to_excel)
openpyxl>=3.1

# Progress bars
tqdm>=4.66
