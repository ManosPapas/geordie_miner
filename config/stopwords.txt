# User stopwords — one word or phrase per line.
#
# Standard English stopwords (the, and, of, ...) are provided automatically
# by NLTK based on the `language` setting in config.txt — do NOT list them here.
#
# Add corpus-specific stopwords below: journal names, repeated boilerplate,
# author names, your institution, etc. Anything you want stripped before
# topic modelling.
#
# Examples (uncomment to use):
# Journal of Marketing Research
# International Journal of Information Management

# ---------------------------------------------------------------------------
# Modal / auxiliary verbs that NLTK's English list omits.
# NLTK ships "should" and the negative-contraction stems (couldn, wouldn, ...)
# but NOT the bare modals below — so without these lines, "would" and "could"
# survive into your topic models. Delete any you actually want to keep.
# Multi-word entries work as phrases (the whole phrase is removed), e.g. "et al".
# ---------------------------------------------------------------------------
would
could
may
might
must
shall
also
et al
